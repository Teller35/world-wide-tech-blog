<div class="comments">
  {{#each this}}
    <section class="comment">
      <div>
        {{user.username}}
        on
        {{format_date created_at}}
      </div>
      <div class="comment-text">
        {{input_text}}
      </div>
    </section>
  {{/each}}
</div>