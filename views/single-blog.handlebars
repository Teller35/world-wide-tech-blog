{{> blog-info blog}}

{{#if loggedIn}}
<form class="comment-form">
    <div>
        <textarea class=" textarea" name="comment-input"></textarea>
    </div>
    <div>
        <button type="submit" class="btn">Add Comment</button>
    </div>
</form>
{{/if}}
{{> comments blog.comments}}
{{#if loggedIn}}
<script src="/javascript/comment.js"></script>
{{/if}}